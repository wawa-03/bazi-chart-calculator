# 观历：用户与命书归档数据库结构审计

**审计日期：** 2026-08-22  
**范围：** `users`、`savedArchives` 两张已部署表，及其保存、列表、载入、删除查询路径。  
**结论：** **当前结构足以支撑早期个人档案保存，列表索引已经正确命中；不建议为了当前 1 名用户、0 条归档记录而立即添加更多查询索引。** 更高优先级是缩小敏感住址的返回范围、将关键历法输入和规则快照结构化，以及清理重复的 `userId` 单列索引。

## 一、实际数据库状态

| 项目 | 审计结果 | 评价 |
| --- | --- | --- |
| 当前规模 | `users` 为 1 条，`savedArchives` 为 0 条。 | 当前不存在需要用更多索引解决的容量压力。 |
| 用户主键 | `users.id` 为自增主键；`openId` 有唯一 B-tree 索引。 | 认证身份查找完整且足够。 |
| 归档关系 | `savedArchives.userId` 外键指向 `users.id`，并使用 `ON DELETE CASCADE`。 | 用户数据清理可级联，关系完整性设计合理。 |
| 当前列表索引 | 已有 `(userId, createdAt)` 复合 B-tree 索引。 | 用户维度倒序列出档案的核心查询已获覆盖。 |
| 执行计划 | `WHERE userId = ? ORDER BY createdAt DESC` 使用 `saved_archives_user_created_idx`，并保持倒序。 | 现有索引与代码中的列表访问模式匹配。 |

> MySQL 对复合索引可使用其**左前缀**；因此 `(userId, createdAt)` 同时可服务于按 `userId` 过滤，及按创建时间排序的查询。[1]

## 二、现有字段模型评价

| 表 / 字段 | 现状 | 审计判断 | 建议优先级 |
| --- | --- | --- | --- |
| `users.openId` | `varchar(64)`、唯一索引 | 合理；是账户身份边界。 | 保持不变 |
| `users.email`、`name` | 不建索引 | 当前无按邮箱或姓名检索的产品路径。 | 不加索引 |
| `savedArchives.id` | 自增主键 | 单条载入/删除先按主键定位，性能足够。 | 保持不变 |
| `savedArchives.userId` | 外键、复合索引首列 | 合理；保障归档隔离与列表查询。 | 保持不变 |
| `label`、`targetYear` | 结构化列 | 可支撑列表摘要与年份筛选。 | 视筛选功能再扩展索引 |
| `birthDatetime` | `varchar(32)` | 能保留输入字符串，但不利于日期筛选、校验与时区表达。 | 中 |
| `inputJson` | `text` | 方便早期快速演进，但关键排盘参数无法在数据库侧校验或查询。 | 高 |
| `profileJson` | `text`，含姓名、出生地、门牌号住址 | 资料整体返回，导致列表接口也会带出敏感地址。 | **最高** |
| 缺少规则快照 | 未保存 `nextJie`、开放月份、规则版本及命书正文快照。 | 将来调整节气规则或文案后，历史档案不能稳定复现。 | 高 |

## 三、索引审计结论

### 当前可保留的索引

`users` 的主键和 `openId` 唯一索引均为必要索引。`savedArchives` 的主键与 `(userId, createdAt)` 复合索引也应保留。前者支持单记录定位，后者准确覆盖“某用户的档案列表按创建时间倒序”的实际查询。

### 当前不建议新增的索引

不建议立即给 `label`、`birthDatetime`、`targetYear`、`createdAt` 单独建索引；当前表没有归档数据，且产品尚未提供全文搜索、跨用户检索、出生日期范围检索或按年份筛选。额外索引会增加写入与维护成本，收益有限。

### 值得清理的重复索引

实际库中同时存在外键自动建立的 `savedArchives_userId_users_id_fk (userId)` 与业务复合索引 `saved_archives_user_created_idx (userId, createdAt)`。后者以 `userId` 为左前缀，已能承担外键所需的引用列索引和用户过滤；因此前者**很可能重复**。在确认 TiDB 当前外键约束可由复合索引继续支撑后，可通过迁移删除单列 `userId` 索引，降低每次保存档案的索引写入成本。[1] [2]

### 仅在功能出现后再加的索引

| 未来功能 | 推荐索引 | 说明 |
| --- | --- | --- |
| “按年份筛选我的命书” | `(userId, targetYear, createdAt)` | 仅在该筛选真正上线后添加。 |
| “仅看活跃/归档状态” | `(userId, status, createdAt)` | 仅在引入状态字段后添加。 |
| 跨年节气提醒任务 | `(targetYear, status)` 或独立任务表索引 | 不应依赖 JSON 字段扫描。 |

## 四、推荐的结构调整

### 第一优先级：分离摘要与敏感详情

当前 `listSavedArchives()` 直接查询整行，客户端历史列表实际上会收到 `inputJson` 与 `profileJson`，其中含门牌号住址。建议先不拆表也完成第一步优化：**列表接口仅选择 `id`、`label`、`targetYear`、`createdAt`；新增受保护的 `get(id)` 接口，在用户主动载入时才返回详细资料。** 这项改动不需要新增索引，却能立刻缩小敏感字段的网络与浏览器内存暴露范围。

第二步可把住址移至一对一的 `archivePrivateDetails` 表，只允许按 `archiveId + userId` 读取，并将详细住址改为应用层加密的密文。这里不建议把可搜索字段塞进密文；若未来需要城市级筛选，应单独保存最低必要粒度的地区代码或城市名。

### 第二优先级：结构化关键排盘字段，保留 JSON 快照

建议将常用、需校验或需复现的字段从 JSON 提升为列，同时保留 JSON 作为可扩展快照。推荐目标如下：

| 字段 | 建议类型 | 原因 |
| --- | --- | --- |
| `birthLocalAt` | `datetime` | 支持格式校验与日期运算。 |
| `birthTimezone` | `varchar(64)` | 明确排盘业务时区，例如 `Asia/Shanghai`。 |
| `birthLongitude` | `decimal(7,4)` | 避免浮点误差，并支持经度边界校验。 |
| `birthLatitude` | `decimal(6,4)` | 同上。 |
| `gender` | `enum('male','female')` | 与计算输入约束一致。 |
| `ruleVersion` | `varchar(32)` | 历法和节气规则变更后，仍可解释历史档案。 |
| `nextJie` | `varchar(8)` | 记录生成时使用的下一节。 |
| `openMonthsJson` | `json` | 保存当时实际开放的未来农历月范围。 |
| `manualSnapshotJson` | `json` / `text` | 固化当年总盘和月卷文字，避免前端文案改版后历史内容漂移。 |

如果继续使用 JSON 做筛选，不应假定 JSON 内容可被直接高效索引；官方文档建议通过生成列抽取需要检索的标量后再建索引。[3]

### 第三优先级：为档案生命周期增加可治理性

若产品将保存详细地址，建议加上 `dataRetentionUntil`、`deletedAt` 或明确的“永久删除”审计事件。当前物理删除符合用户立即删除的期待，但没有保留数据治理层面的处理时间与策略标记。是否需要软删除应由产品的恢复需求决定；若承诺“永久删除”，则不应把软删除伪装成永久删除。

## 五、建议的迁移顺序

1. **先改接口投影，不改表：** 列表只返回摘要，新增按 ID 读取详情接口。
2. **再加结构化列：** 保留旧 JSON；后台迁移时填充 `birthLocalAt`、坐标、性别、规则快照。
3. **验证后切读新列：** 通过回归测试和现有档案抽样核对后，改写保存与读取路径。
4. **最后处理重复索引：** 在预发布库用 `EXPLAIN` 验证外键、列表和删除路径后，删除单列 `userId` 索引。

该顺序避免一次性重写所有历史数据，也能把用户可感知风险优先降下来。

## 参考

[1] [MySQL：Multiple-Column Indexes](https://dev.mysql.com/doc/en/multiple-column-indexes.html)

[2] [MySQL：FOREIGN KEY Constraints](https://dev.mysql.com/doc/refman/8.4/en/create-table-foreign-keys.html)

[3] [MySQL：Secondary Indexes and Generated Columns](https://dev.mysql.com/doc/refman/8.4/en/create-table-secondary-indexes.html)
