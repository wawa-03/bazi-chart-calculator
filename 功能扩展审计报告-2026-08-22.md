# 观历八字工具：节气未来月卷与私有归档审计

**审计日期：** 2026-08-22  
**审计范围：** 节气驱动的未来农历月卷、示例隐私替换、年度归档持久化与访问控制。  
**总体结论：** **已实现核心要求，且当前代码与验证结果一致。** “2026 年 8 月查看 2026 年”这一目标场景中，系统以白露为下一节，将农历九月、十月、冬月、腊月设为可读，农历正月至八月保持锁定。

## 一、逐项结论

| 审计项 | 结论 | 证据与说明 |
| --- | --- | --- |
| 节气而非自然月解锁 | **通过** | `annualCycle.ts` 通过历法库取得 `getNextJie()`，将十二个“节”映射为下一段可预览的传统月卷；界面不再使用公历自然月决定当年可读范围。 |
| 2026 年 8 月的范围 | **通过** | 自动测试断言 `2026-08-22` 的下一节为白露，并返回 `[9,10,11,12]`；浏览器 DOM 复验显示农历正月至八月 `disabled=true`，农历九月至腊月 `disabled=false`。 |
| 已开放月卷内容 | **通过** | 浏览器点击农历九月后，具体阅读卡片正常呈现；锁定月按钮不可触发。 |
| 下一年阅读 | **通过** | 选择 2027 年时，十二个月卷均可预览，符合“命书给未来信息”的定位。 |
| 隐私示例姓名 | **通过** | 年度资料默认值与输入占位符均已替换为 **王二小**；不再出现用户样本中的姓名。 |
| 私有持久化 | **基本通过** | 已有账户、`savedArchives` 表、保存/列表/删除接口及 UI。所有接口以认证用户 ID 过滤，删除条件同时要求记录 ID 与用户 ID。 |

## 二、实现判断

> 当前规则是**“下一节之后，开放该年度剩余的未来月卷”**，而不是“在每个节气到达时逐月放开”。这与您给出的八月查看当年、一次可看农历九月至十二月的例子一致。

当前模块将十二个“节”与月卷起点固定对应。例如，下一节为**白露**时，起点为农历九月，并开放九月至腊月。该行为经单元测试、浏览器按钮禁用状态和已开放卡片三层验证。

## 三、扩展审计：已具备能力与待优化风险

| 领域 | 现状 | 风险等级 | 建议 |
| --- | --- | --- | --- |
| 节气失败回退 | 当历法库未返回节气名称时，代码会回退到“公历月 + 1”的近似值。 | 中 | 删除自然月回退；无法确认节气时应显示“暂不能生成”，避免悄悄违背规则。 |
| 时区一致性 | 节气判断使用浏览器本地时间。不同地区、系统时间被修改或接近节气交界时，可能出现不同结果。 | 高 | 固定以 `Asia/Shanghai` 作为业务时区，并由服务端提供当前时刻或节气判定。 |
| 农历闰月 | 当前目录固定为十二个月，未建立闰月命名与独立月卷策略。 | 中 | 明确闰月是合并到对应月份还是单列，并把该规则写入版本化配置。 |
| 私有地址数据 | 现居详细地址存入 `profileJson`，属于访问受控的明文业务数据；列表接口也会把完整 JSON 返回给已登录浏览器。 | 高 | 列表接口只返回摘要；新增按 ID 读取接口。若长期保存门牌号，建议增加应用层加密与保存期限。 |
| 可重现性 | 已保存档案未保存“当时的下一节、规则版本与可读范围”。规则未来变更后，旧档案可能呈现不同范围。 | 中 | 保存 `ruleVersion`、`nextJie` 与 `openMonths` 快照。 |
| 真实账户联调 | 单元测试已覆盖用户 ID 隔离；未使用真实账户或真实住址进行浏览器保存/载入/删除验证。 | 低 | 发布前由账户持有人用非敏感测试资料完成一次端到端验证。 |

## 四、审计建议的实施优先级

**第一优先级**是将节气判定固定到北京时间，并删除自然月近似回退。这样可以保证“按节气而非自然月”的规则在不同用户设备上保持一致。

**第二优先级**是拆分档案列表与详情读取：列表只返回姓名别名、年份、创建时间和记录 ID；只有在用户明确点击“载入”后，再请求含住址的详情。这样可以降低敏感地址在浏览器内存和网络响应中暴露的范围。

**第三优先级**是保存规则快照与闰月策略。前者保证历史命书可复现，后者使传统历法场景在闰月年份仍保持明确、可解释的行为。

## 五、验证摘要

| 验证方式 | 结果 |
| --- | --- |
| Vitest | 3 个测试文件、5 项断言通过；覆盖节气未来月范围、下一年预览、归档用户隔离和登录退出。 |
| 类型检查 | 通过。 |
| 生产构建 | 通过。 |
| 浏览器核验 | 复验 2026 年八月场景、月卷禁用状态、农历九月内容和桌面/移动端布局。 |

## 参考

[1] [lunar-javascript：历法与节气计算库](https://github.com/6tail/lunar-javascript)

