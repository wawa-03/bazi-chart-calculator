# 全站语言与坐标精度验证记录

**验证日期：** 2026-08-22  
**范围：** 全站语言状态、高精度经纬度输入、排盘、复制文本与 PNG 导出。

| 场景 | 操作 | 结果 |
|---|---|---|
| 桌面全站语言 | 在顶栏依次选择 `zh-CN`、`zh-TW`、`en` | 导航、排盘表单、结果标题、城市搜索与年度阅读入口同步更新。 |
| 移动端全站语言 | 在 375px 宽视口依次选择 `zh-CN`、`zh-TW`、`en` | 顶栏选择器保持可访问；导航、表单、结果、城市搜索与年度阅读入口同步更新。 |
| 城市回填精度 | 端到端选择“北京”候选；受控地点详情返回经度 `116.407396123456`、纬度 `39.904201987654` | 输入框回填完整经纬度；`cityLocationFromPlace` 单元测试也确认坐标按原精度传给排盘表单，不执行四位小数截断。 |
| 高精度坐标排盘 | 输入经度 `116.407396123456`、纬度 `39.904201987654` 后排盘 | 结果区保留并显示浏览器数值精度，没有四位小数步进限制。 |
| 纯文本导出 | 复制上述排盘 | 剪贴板文本含完整的经纬度数值。 |
| PNG 导出 | 下载上述排盘卡，并对 Canvas `fillText` 调用做单元校验 | 浏览器成功产生 PNG 下载；测试确认画布实际写入完整的经纬度字符串。 |

端到端验证由 `scripts/verify-global-locale-and-coordinates.mjs` 执行，覆盖实际城市候选回填、排盘、复制、PNG 下载及桌面/移动端三语矩阵；PNG 画布内容由 Vitest 单元测试补充覆盖。最终共通过 **8 个测试文件、15 个测试**，以及类型检查和生产构建。浏览器采用 JavaScript 数值类型，因而会按 IEEE 754 双精度表示有效坐标；页面不会再主动将输入或计算截断至四位小数。
