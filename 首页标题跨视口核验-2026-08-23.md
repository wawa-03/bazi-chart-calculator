# 首页标题跨视口核验

| 视口 | 已检查内容 | 结果 |
| --- | --- | --- |
| 桌面 `1280 × 720` | “命运，已经选好”标题可见；“开始排盘”按钮位于首屏。 | 通过 |
| 移动 `375 × 812` | “命运，已经选好”完整断行；“开始排盘”按钮位于首屏。 | 通过 |

自动化检查位于 `scripts/verify-free-shell.mjs`。该脚本在两个视口中断言标题可见、文字匹配且标题与主按钮的底部不超过视口高度。最近一次执行通过。
